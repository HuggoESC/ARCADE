//#include "AABB.h"
//#include <iostream>
//#include <vector>
//#include "raylib.h"
//#include "raymath.h"
//using namespace std;
//
//bool AABB::TestAABB(const AABB& box) const
//{
//	return!(Point[0] + width < box.Point[0] || box.Point[0] + box.width < Point[0] ||
//		Point[1] + height < box.Point[1] || box.Point[1] + box.height < Point[1]);
//}
//
//bool AABB::TestPoint(const vector <int>& q) const
//{
//	return (q[0] >= Point[0] && q[0] <= Point[0] + width &&
//		q[1] >= Point[1] && q[1] <= Point[1] + height);
//}

